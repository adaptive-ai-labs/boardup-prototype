import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'BoardUp - Architectural Licensure Exam Preparation Platform',
  description: 'A comprehensive platform designed to help architecture students prepare for their licensure examinations through interactive quizzes, flashcards, and study resources.',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}